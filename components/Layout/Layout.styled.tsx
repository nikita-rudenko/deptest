import { styled } from "stitches.config"

export const LayoutContainer = styled("div", {
  display: "flex",
  flexDirection: "column",
  fontFamily: "$primary",
  background: "$black",
  minHeight: "100vh",
  position: "relative",
})
